import React from "react";
import Button from "../Btn/Button/Button";

const BottomPopUp = () => {
  return (
    <div>
      공유하기
      <button>공유</button>
      <Button type={"brand"} size={"s24"} />
    </div>
  );
};

export default BottomPopUp;
